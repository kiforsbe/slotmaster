{
  "name": "slotmaster",
  "version": "0.1.0",
  "description": "Browser-based slot machine games.",
  "private": true,
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": { "test": "node --test tests/*.mjs" }
}
